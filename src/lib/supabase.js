/**
 * Supabase has been replaced by the Manus backend.
 * This stub exists so any remaining imports of hasSupabase() compile
 * without errors. Returns true since we now have a working backend.
 */
export const supabase = null;
export function hasSupabase() {
  return true;
}
